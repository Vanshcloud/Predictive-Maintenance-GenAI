# GitHub renders a "Sponsor" button only when a field here is populated.
# Nothing is set: this project takes no funding. The file exists so the
# option is documented rather than rediscovered.
#
# github: [Vanshcloud]
# ko_fi:
# custom: []
